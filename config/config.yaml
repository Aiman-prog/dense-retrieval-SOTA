# --- Reproducibility ---
seed: 42

# --- Global Model Settings ---
model:
  base_model: "BAAI/bge-m3"
  pooling: "cls"  # BGE-M3 uses CLS pooling for dense retrieval
  normalize: true
  temperature: 0.02  # Standard for dense retrieval contrastive learning
  query_max_len: 1024  # Measured (BGE-M3 tokenizer): BRIGHT eval queries are long reasoning prompts (median ~666 tok); 256 covered ~0% of them, 1024 covers ~95%. Train queries (reasonir, median ~79) fit easily. Dynamic padding => negligible cost on short queries.
  passage_max_len: 512  # Measured token coverage (BGE-M3 tokenizer): ~90% of reasonir/BRIGHT passages retained at 512 vs only ~57% at 128. 128 truncated 70-79% of theoremqa/aops/leetcode passages. Re-encode stale index after changing.

# --- Paths Configuration ---
paths:
  data_dir: "data"
  processed_dir: "data/processed"
  bright_cache: "data/bright"
  models_dir: "models"
  results_dir: "results/bright_benchmark"

# --- Data Sources ---
data:
  bright:
    name: "xlangai/BRIGHT"
    examples_config: "Gemini-1.0_reason"
    cache_dir: "data/bright"
  reasonir:
    name: "reasonir/reasonir-data"
    subset: "hq"
    train_file: "train_reasonir.jsonl"
  mixed_training:
    msmarco_samples: 83030   # 23.7% - Public data bridge + calibration (prevent forgetting)
    vl_samples: 149970       # 42.8% - ALL available clean VL (length generalization)
    hq_samples: 97000        # 27.7% - Nearly all HQ (reasoning-intensive queries)
    # Total: ~350k samples | Note: VL corrupted before index 95k, only 150k clean samples available
  msmarco:
    name: "sentence-transformers/msmarco-distilbert-margin-mse-mean-dot-v1"
    subset: "triplet"

# --- Training Recipes ---
training:
  logging_steps: 100
  overwrite_output_dir: true
  dataloader_num_workers: 4
  inbatch:
    model_name: "inbatch_mixed_bge_m3"
    batch_size: 64
    learning_rate: 1e-5
    num_epochs: 2
    train_group_size: 2
    bf16: True
    dataloader_num_workers: 4
    warmup_ratio: 0.1
    weight_decay: 0.01
    max_grad_norm: 1.0
  crossbatch:
    model_name: "crossbatch_mixed_bge_m3_epoch2"
    batch_size: 16
    target_batch_size: 2048
    per_device_batch_size: 64
    gradient_accumulation_steps: 16
    gc_q_chunk_size: 16
    gc_p_chunk_size: 16
    learning_rate: 1e-5  # Lower LR for BGE-M3 continued training
    warmup_ratio: 0.1
    weight_decay: 0.01
    num_epochs: 2  # Quick validation run
    train_group_size: 2
    grad_cache: true
    bf16: true
    max_grad_norm: 1.0
    logging_steps: 10
    dataloader_num_workers: 4
  grass:
    # σ estimator is selected at the CLI: --uncertainty {mc_dropout, ema}
    model_name: "grass_mixed_bge_m3"   # base name — code appends _mc_dropout or _ema
    base_model: "/scratch/aimanabdulwaha/dense-retrieval-SOTA/models/inbatch_mixed_bge_m3"

    # --- Shared ---
    P: 200             # candidates retrieved from stale ANN per query
    L: 25              # shortlist size after fresh current-model rerank (L <= P) — [S9] halved from 50
    m: 1               # negatives per query (= train_group_size - 1)
    lambda_val: 1.0    # uncertainty weight in g = s_hat + lambda * sigma
    mc_batch_size: 1024 # encoding batch size (controls peak GPU memory during mining)
    learning_rate: 1e-5
    num_epochs: 2
    train_group_size: 2
    bf16: true
    dataloader_num_workers: 2
    warmup_ratio: 0.1
    weight_decay: 0.01
    max_grad_norm: 1.0
    save_strategy: "steps"
    save_steps: 1000   # [S12] doubled from 500 — saves ~2 min in checkpoint I/O per 3 epochs
    save_total_limit: 1
    logging_steps: 100
    eval_top_k: 10
    per_device_eval_batch_size: 256

    # --- EMA-specific (used when --uncertainty ema) ---
    ema_alpha: 0.999

    # --- MC-dropout-specific (used when --uncertainty mc_dropout) ---
    T: 3               # MC-dropout forward passes
    mc_dropout_p: 0.3  # dropout probability for MC passes (0.1 default is too weak)
    batch_size: 64
    max_pool_per_query: 200  # FAISS candidate pool cap per query
  ance:
    base_model: "/scratch/aimanabdulwaha/dense-retrieval-SOTA/models/inbatch_mixed_bge_m3"
    model_name: "ance_mixed_bge_m3"
    train_group_size: 2       # 1 negative — matches original ANCE paper (Table 3)
    mining_depth: 200
    total_epochs: 3           # async design: Trainer runs continuously; Inferencer refreshes in parallel
    learning_rate: 1e-5
    batch_size: 64
    per_device_eval_batch_size: 512
    bf16: true
    dataloader_num_workers: 2
    warmup_ratio: 0.1
    weight_decay: 0.01
    max_grad_norm: 1.0
    gradient_checkpointing: false
    save_steps: 500
    save_total_limit: 1
    logging_steps: 100
    eval_top_k: 10
    data_gen_poll_interval: 60
    corpus_file: "reasonir_corpus.jsonl"
    train_queries_file: "train_queries.jsonl"
    train_qrels_file: "train_qrels.txt"
    mixture_dir: "training_mixture"
    temp_workdir: "temp_ance"
    setup_mode: "reasonir_mixture"
    eval_corpus_file: null
    eval_queries_file: null
    eval_qrels_file: null
    eval_metric: "ndcg_cut_10"


# --- Evaluation Settings (Restored & Cleaned) ---
evaluation:
  eval_domains:
    - "biology"
    - "earth_science"
    - "economics"
    - "psychology"
    - "robotics"
    - "stackoverflow"
    - "sustainable_living"
    - "pony"
    - "leetcode"
    - "aops"
    - "theoremqa_theorems"
    - "theoremqa_questions"
  metrics:
    - "MRR"
    - "NDCG@10"
    - "Recall@1000"
  top_k: 1000
  batch_size: 128
  trec_eval_path: "trec_eval"
  bf16: True
  dataloader_num_workers: 4
  bm25:
    k1: 0.9
    b: 0.4
