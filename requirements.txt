# ============================================================================
# Requirements for LOCAL DEVELOPMENT ONLY
# ============================================================================
# For HPC/DelftBlue: Use Singularity containers (see setup.sh)
# This file matches the container environment for local testing
# ============================================================================

# --- Core Deep Learning ---
torch>=2.6.0  # Local dev (Mac/Python 3.13+). HPC actually runs torch 2.10.0+cu128 from
              # ~/.local, NOT the container's 2.1.0 — see docs/DELFTBLUE_ENVIRONMENT.md (P7).
transformers>=4.36.0,<5  # REQUIRED: peft needs Cache class from 4.36.0+; <5 because v5 requires PyTorch>=2.4
sentence-transformers>=2.2.0,<3.0.0
datasets>=2.14.0,<3.0.0
accelerate>=0.25.0  # REQUIRED: Tevatron needs clear_device_cache from 0.25.0+
safetensors>=0.4.0
peft>=0.7.0  # Parameter-efficient fine-tuning

# --- Dense Retrieval Framework ---
# NOTE: Install from GitHub to match patched version on HPC
git+https://github.com/texttron/tevatron.git@8f31cd8  # CRITICAL: Use this exact commit, then apply patches (see DELFTBLUE_SETUP.md)
# PINNED TO A COMMIT, not to main. GradCache's version string is a static 0.1.0 on
# every commit, so the version pin below would be meaningless -- the commit IS the
# identity. helpers.patch_tevatron_loss rebinds `SimpleContrastiveLoss` and
# `DistributedContrastiveLoss` by name inside tevatron.retriever.gc_trainer, so an
# upstream rename would silently drop temperature scaling rather than fail. This SHA is
# what the unpinned `main` resolved to in the project venv (recovered from pip's
# direct_url.json via `python scripts/patch_tevatron.py --verify`), and it is VERIFIED
# to be what DelftBlue already runs: all 11 files under grad_cache/ are byte-identical
# to the cluster's copy (installed 2026-02-09 from archive/main.tar.gz). Pinning it
# therefore freezes the working stack rather than changing it.
git+https://github.com/luyug/GradCache.git@906f03835fbc183132a9db32612a9e8f180ca3b4  # Required for --grad_cache (cross-batch negatives)

# --- Vector Search & Retrieval ---
faiss-cpu  # Local dev (Mac). HPC uses faiss-gpu via requirements-hpc.txt + Singularity
pyserini==1.5.0  # exact: the Lucene version, and so index compatibility, rides on it
pytrec-eval-terrier  # Python bindings for TREC evaluation (maintained fork)

# --- Utilities ---
numpy>=2.0.0  # Local dev (Python 3.13+ requires numpy 2.x). HPC container provides its own numpy.
pandas>=2.0.0
pyyaml>=6.0
tqdm>=4.65.0
matplotlib>=3.7.0
importlib_metadata
spacy>=3.5.0  # NLP utilities

# ============================================================================
# INSTALLATION NOTES
# ============================================================================
# 1. Install PyTorch first (with CUDA if available):
#    pip install torch==2.1.0 --index-url https://download.pytorch.org/whl/cu118
#
# 2. Install requirements:
#    pip install -r requirements.txt
#
# 3. Apply Tevatron patches (see DELFTBLUE_SETUP.md or run setup.sh)
#
# 4. For HPC deployment, use setup.sh instead - it handles everything automatically
# ============================================================================

