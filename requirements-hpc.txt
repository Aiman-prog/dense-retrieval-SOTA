# ============================================================================
# Requirements for HPC (DelftBlue) Singularity Container
# ============================================================================
# These are installed INTO ~/.local via: pip install --user -r requirements-hpc.txt
#
# ⚠️ REALITY CHECK (verified 2026-08-20, defect P7):
#   The container provides CUDA + a torch 2.1.0 that NOTHING imports, and no
#   transformers at all. ~/.local also holds torch==2.10.0+cu128 (pulled in as a
#   dependency on 2026-02-22, not requested), which shadows the container's torch
#   for every job. torchvision 0.16.0 / torchaudio 2.1.0 remain at the 2.1-era ABI
#   and would fail if imported — nothing in this repo imports them.
#   The pins below are what actually works. Do NOT 'fix' them upward to match
#   torch 2.10; every result in the repo was produced with exactly these versions.
#   Resolved environment: env_delftblue_actual.txt
# Tevatron & GradCache are installed separately (from tarballs, no git needed)
#
# IMPORTANT: All versions are pinned for PyTorch 2.1.0 compatibility.
# Newer versions of transformers/accelerate/peft use PyTorch 2.2+ APIs.
# ============================================================================

# --- Core ML Libraries (pinned for PyTorch 2.1 compatibility) ---
transformers==4.40.2           # Last version fully compatible with PyTorch 2.1
accelerate==0.30.1             # 0.31+ uses PyTorch 2.2+ pytree APIs
datasets==2.19.2
safetensors==0.4.3
peft==0.10.0                   # Parameter-efficient fine-tuning

# --- Vector Search & Evaluation ---
faiss-gpu>=1.7.0                   # FAISS vector search (GPU, CUDA 11.8)
pytrec-eval-terrier                # TREC evaluation metrics (MRR, NDCG, Recall)
pandas>=2.0.0                      # Data manipulation (qrels, results, preprocessing)
matplotlib>=3.7.0                  # Plotting (recall curves)

# --- Utilities ---
pyyaml>=6.0
